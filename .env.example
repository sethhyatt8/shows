# TMDB API key (v3) — for `npm run enrich` only (agent/dev use).
TMDB_API_KEY=

# Same Supabase project as Emily (Settings → API in dashboard).
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key

# Site password (not committed). For GitHub Pages, add the same name as a repository Secret.
VITE_APP_PASSWORD=
